// Faça um algoritmo para receber um número qualquer e informar na tela se é par ou ímpar.

let numeroConfuso = (document.getElementByID("#numeroConfuso"))

function revelarNumero = (numeroConfuso) {
    if ((numeroConfuso / 2) % 0) {
        alert('o número ("numeroConfuso") é par')
    }
}
